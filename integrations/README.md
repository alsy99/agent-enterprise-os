# Integrations

## MCP

Week 1: filesystem + (optional) GitHub + browser. Config: `mcp/mcp.json`.

## Models

Role router + provider catalog + LiteLLM proxy: [`models/`](./models/) · [`litellm/`](./litellm/).

Later (not now): n8n front door, Temporal, Graphiti, OpenClaw channels.
