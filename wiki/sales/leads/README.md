# Leads (public-repo policy)

**Decision (before lead #2):** this GitHub repo stays public.

| Allowed in git | Not allowed in git |
|---|---|
| Company-level research (`wiki/sales/leads/<slug>.md`) | Personal emails, phone numbers, LinkedIn URLs of individuals |
| Outreach drafts with `{{name}}` / `{{sender}}` placeholders | Real `To:` addresses or “email already sent to X@…” |
| Public product/docs citations | CRM notes, call recordings, private Slack |

Put anything with real prospect contact data in `*.private.md` (gitignored) or keep it only in a private fork / local notes.

Clay (`clay.md`) is company-level dogfood — fine public.  
Lead #2 (`devtool-gtm.md`) peer (skills pack).  
Leads #3–#5 buyers: `indebtio`, `openstatus`, `appear`.  
See `traces/runs/2026-09-07-n5-intervention-summary.md` for minutes.
